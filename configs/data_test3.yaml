# Evaluation config: the `val:` key points at Test 3 so that `model.val()`
# reports metrics for that held-out acquisition session.
path: ../datasets
train: train/tiles/train/images
val: test_3/tiles/images
nc: 2
names: ['defect', 'particle']
